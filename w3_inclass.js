//comment out one line;

/*
in between
you can
comment out 
a whole block
*/

// = declare value
let x = "1" //a string, javascript statement
x == 1
// == loose equivalent (reacsting string as number)
x === 1
// === strict equivalent (value and type)

if (condition) {
    //block of code when true;
} else {
    // block of code when false;
}

//lowercase Camel case is the default, ex. runFunction()
// let statement declaration is most common, let set variable in a block
